import { Dimensions } from 'react-native';

const { width, height } = Dimensions.get('window');

export default {
  navbarHeight: 30,
  indent: 24,
  halfIndent: 12,
  padding: 8,
  textPadding: 5,
  width,
  height,
};
